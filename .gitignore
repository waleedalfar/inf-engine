# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Model weights (large; downloaded via scripts/download_weights.py)
weights/

# Profiling traces / large artifacts
*.json.gz
*.pt.trace.json
traces/
bench/results/engine_trace.json

# Keep plots tracked, but ignore scratch
plots/scratch/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code: per-user local permissions, not shared team config.
.claude/settings.local.json

# Private incident/debugging write-ups — kept locally for reference, not for
# the public repo. The distilled, actionable version of each lesson lives in
# .claude/skills/ (tracked) so future sessions still apply it automatically.
docs/lessons/
